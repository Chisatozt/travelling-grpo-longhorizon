# SFT configuration view

The project-level SFT configuration is:

- configs/sft/qwen3_customized.yaml
- configs/sft/task_alignment.example.json

The Qwen3.5 veRL trainer configuration remains under
verl/trainer/config/travel_qwen35_sft.yaml because it is part of the Hydra
package configuration.
