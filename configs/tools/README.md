# Tool configuration view

The canonical TravelGym tool configurations are:

- configs/tools/interact_tool_config.yaml
- configs/tools/interact_tool_schema.yaml

The AgentLoop configuration still loads
verl.tools.interact_tool.InteractTool. The Python import path is unchanged.
