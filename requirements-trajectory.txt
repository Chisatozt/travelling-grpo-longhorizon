# Lightweight local environment for collecting TravelGym SFT trajectories.
# The full requirements.txt is intentionally not used here: it includes the
# CUDA/Ray/SGLang/vLLM training stack, which should be installed on the GPU
# server after the data collection phase.
-e gyms/TravelGym

# Evaluation and SFT-data preparation
pandas>=2.1
pyarrow>=19.0.0
datasets>=2.18
transformers>=4.45,<5
python-dotenv>=1.0,<2
swanlab>=0.9.8,<1.0

# eval/eval.py imports the Gemini adapter even when the selected actor is an
# OpenAI-compatible model.  Keeping it here makes the evaluator importable.
google-genai>=1.0
